#include "planner_D.h"
using namespace planner;

uint8_t planner_D::initialize(const std::string& config_path)
{
    return 0;
}

void planner_D:: run( const Location& locInfo,
                    const VehicleState& VehicleState,
                    PlannerDataFlow& PlannerDataFlow) 
                    {

                    }

void planner_D:: noRun(const Location& locInfo,
                    const VehicleState& VehicleState) 
                    {

                    }