#include "planner_B.h"
using namespace planner;

uint8_t planner_B::initialize(const std::string& config_path)
{
    return 0;
}

void planner_B:: run( const Location& locInfo,
                    const VehicleState& VehicleState,
                    PlannerDataFlow& PlannerDataFlow) 
                    {

                    }

void planner_B:: noRun(const Location& locInfo,
                    const VehicleState& VehicleState) 
                    {

                    }