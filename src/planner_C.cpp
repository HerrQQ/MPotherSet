#include "planner_C.h"
using namespace planner;

uint8_t planner_C::initialize(const std::string& config_path)
{
    return 0;
}

void planner_C:: run( const Location& locInfo,
                    const VehicleState& VehicleState,
                    PlannerDataFlow& PlannerDataFlow) 
                    {

                    }

void planner_C:: noRun(const Location& locInfo,
                    const VehicleState& VehicleState) 
                    {

                    }